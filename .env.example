# NbS Rural Scan - Local Environment Configuration
# Copy this file to '.env' and adjust the paths for your local system.

# OneDrive Library Path (where copyrighted PDFs are permanently stored)
ONEDRIVE_LIBRARY_PATH="/Users/pstewarda/Library/CloudStorage/OneDrive-CGIAR/ClimateActionNetZero/1_Projects/D591_Rural-Scan_NBS/2_Technical_&_Data/library/"

# Local Corpus Path (where PDFs are staged for ingestion, gitignored)
LOCAL_CORPUS_PATH=".cache/corpus/"

# Local Ingest Cache Path (where parsed JSON document indexes are stored, gitignored)
LOCAL_INGEST_CACHE=".cache/ingest/"
