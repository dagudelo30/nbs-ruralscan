.DS_Store

# Python / uv
.venv/
__pycache__/
*.pyc
.ruff_cache/

# Ingestion cache — extracted full text of copyrighted PDFs (never committed)
.cache/

# Local environment configuration and secrets
.env
.env.local
.env.*
!.env.example



# one-off contamination-vector scripts — never commit
scratch/

# contaminated registers pulled out during 2026-06 purge — reference only
schema/registers/_quarantine/

# intermediate extraction staging (units land in EV register)
pipeline/staging/

# OneDrive-derived stocktake text (canonical stays on OneDrive)
reference/stocktake/_local/

# adversarial relationship-verification working files
pipeline/verify/

# Track-2 candidate/shard inbox (pre-merge)
pipeline/inbox/

# verify-flag review worklist (work artifact; review.py is the tool)
pipeline/review/
