"""Schema tooling — build, validate, and snapshot the machine-readable schema.

- ``structure``  — validate data files against the frozen column manifest.
- ``generate``   — generate the typed JSON from the CSV source of truth.
- ``freeze``     — snapshot/verify registers + ingest caches for reproducible runs.
"""
